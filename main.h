#ifndef MAIN_H
#define MAIN_H

#include <stdarg.h>
#include <stdlib.h>
#include <unistd.h>

int _putchar(char c);
int _strlen(char *s);
int _strlenc(const char *s);
int print_char(int arg);
int _printf(const char *format, ...);
int print_str(char *str);
int print_perc(void);
int _printf(const char *format, ...);

#endif
